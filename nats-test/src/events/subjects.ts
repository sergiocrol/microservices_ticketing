// enum is something similar to an object. Basically we are gonna define some properties
// and everytime we say that someting is a type of subject, it should be one of those listed values.
export enum Subjects {
  TickedCreated = 'ticket:created',
  OrderUpdated = 'order:updated'
}